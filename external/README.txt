This folder contains files that are needed for reproduction but are not part of the git repository.

This is usually datasets that are produced by the code in the git repo, but we don't want to put .csv in the repo.
